import React from "react"

const DisplayMode = React.createContext({})

export const DisplayModeProvider = DisplayMode.Provider

export const DisplayModeConsumer = DisplayMode.Consumer

export default DisplayMode